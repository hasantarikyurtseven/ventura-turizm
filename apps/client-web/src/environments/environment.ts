/**
 * Development ortamı ayarları.
 * `ng serve` ile çalıştırıldığında bu dosya kullanılır.
 * client-api localhost:3002'de çalışıyor olmalı.
 */
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:3002',
};
