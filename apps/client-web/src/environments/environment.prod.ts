/**
 * Production ortamı ayarları.
 * Canlıya alındığında bu dosya kullanılır.
 * apiBaseUrl, deploy edilen domain'e göre güncellenmelidir.
 */
export const environment = {
  production: true,
  apiBaseUrl: 'https://api.venturaturizm.com',
};
